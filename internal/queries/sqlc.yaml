version: "2"
sql:
  - schema: "."
    queries:
      - "queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "dbsqlc"
        sql_package: "pgx/v5"
        out: "."
        emit_pointers_for_null_types: true
        emit_methods_with_db_argument: true
